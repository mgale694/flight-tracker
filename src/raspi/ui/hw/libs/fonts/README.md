"""Font Files

This directory should contain the Font.ttc file for the display.

You can use any TrueType font file. If you don't have one:

1. On macOS: Copy a system font
   cp /System/Library/Fonts/Supplemental/Courier\ New.ttf Font.ttc

2. On Linux: Copy DejaVu Sans Mono
   cp /usr/share/fonts/truetype/dejavu/DejaVuSansMono.ttf Font.ttc

3. Or download a free monospace font online

The code will fallback to system fonts if Font.ttc is not found.
"""
