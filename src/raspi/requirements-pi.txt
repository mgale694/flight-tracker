# Raspberry Pi Hardware Requirements
# Install these ONLY on actual Raspberry Pi hardware

# GPIO and SPI interface. rpi-lgpio provides the RPi.GPIO-compatible module;
# installing both packages causes a conflict on current Raspberry Pi OS.
spidev>=3.6
gpiozero>=2.0

# GPIO library backend (for gpiozero)
lgpio>=0.2.0.0
rpi-lgpio>=0.4
