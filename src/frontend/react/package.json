{
  "name": "frontend",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "npm run build && npm run serve",
    "build": "npm run clean && npm run build:ts && npm run copy:assets",
    "build:ts": "tsc",
    "copy:assets": "mkdir -p dist && cp -r public/* dist/ 2>/dev/null || true && cp index.html dist/",
    "serve": "npx http-server dist -p 5173 -a 0.0.0.0 --cors -c-1",
    "lint": "echo 'Linting disabled for ARM compatibility'",
    "clean": "rm -rf dist"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/react": "^19.1.8",
    "@types/react-dom": "^19.1.6",
    "http-server": "^14.1.1",
    "typescript": "~5.8.3"
  }
}
